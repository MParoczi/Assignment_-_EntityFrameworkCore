@model dynamic
@{
    ViewData["Title"] = "Posts by";
}

<div class="text-left">
    <h1 class="text-info">@Model.User.FirstName @Model.User.LastName</h1>
    @foreach (Post post in Model.Posts)
    {
        <h2 class="font-italic">"@post.Content"</h2>
    }
</div>
